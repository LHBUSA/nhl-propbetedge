# Server-side only (Vercel project env). Never prefix these with VITE_.
# PropSports API base the /api/nhl proxy forwards to.
PROPSPORTS_BASE_URL=
# Owner dashboard secret the proxy attaches (preferred), or a PropSports API key.
PROPSPORTS_DASHBOARD_SECRET=
PROPSPORTS_API_KEY=
